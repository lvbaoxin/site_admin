const base_url = 'http://localhost:8000/'

const API = {
   role: {
      list: base_url + 'role/list',
   },
   blog: base_url + 'blog/',
   login: base_url + 'login/',
   user: base_url + 'user/',
}

export default API

